# UI Crate

This crate provides an implementation of some useful widgets that are based on `ratatui` but provide additional handling of events or features currently not provided by the base widgets, e.g. horizontally and vertically scrollable tables.

Currently the crate provides the following widgets:

* Input Field
* Selection
* Table

Please refer to the example for some demonstrations.
