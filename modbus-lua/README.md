# Lua Crate

This crate provides an abstract interface to handle a lua runtime based on `mlua` for execution of custom lua scripts. It additionally provides the traits for simple addition of new lua modules to call given rust components from the lua runtime.
