# Register Crate

This crate provides the register layer over a memeory backend. It handles the decode/encoding of registers fron/into the memory backend while taking the format and endianess into account.
